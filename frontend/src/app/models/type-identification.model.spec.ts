import { TypeIdentification } from './type-identification.model';

describe('TypeIdentification', () => {
  it('should create an instance', () => {
    expect(new TypeIdentification()).toBeTruthy();
  });
});
